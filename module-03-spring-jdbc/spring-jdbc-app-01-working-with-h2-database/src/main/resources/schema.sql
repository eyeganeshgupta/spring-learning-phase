CREATE TABLE Students (student_id INT PRIMARY KEY, first_name VARCHAR(50), last_name VARCHAR(50), email VARCHAR(100), gender VARCHAR(10), gpa DECIMAL(3,2), credits_earned INT, address VARCHAR(200), city VARCHAR(50), state VARCHAR(50), zip_code VARCHAR(10), country VARCHAR(50), phone_number VARCHAR(20), emergency_contact_phone VARCHAR(20));
